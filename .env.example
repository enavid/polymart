# Template for backend/.env. Run `make env` to copy this with defaults.
# Native dev uses localhost; the Docker stack overrides the DB/Redis hosts.
# Never commit the real .env.

# --- Django -----------------------------------------------------------------
DJANGO_SETTINGS_MODULE=config.settings.dev
DJANGO_SECRET_KEY=insecure-dev-key-change-me
DJANGO_DEBUG=true
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
DJANGO_LOG_LEVEL=INFO
DJANGO_JSON_LOGS=false
# Browser origins allowed to call the API with credentials (cookie-JWT). The dev
# settings already whitelist localhost:3000; set this only for non-dev settings
# (prod/staging), comma-separated, e.g. https://shop.example.com
# CORS_ALLOWED_ORIGINS=

# --- Database ---------------------------------------------------------------
# Native dev connects to localhost; the same POSTGRES_PORT is used to publish
# the Docker container, so changing it here is enough to avoid a port clash.
# (Set DATABASE_URL explicitly to override entirely, e.g. in prod/CI.)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=polymart
POSTGRES_PASSWORD=polymart
POSTGRES_DB=polymart

# --- Redis / Celery ---------------------------------------------------------
REDIS_HOST=localhost
REDIS_PORT=6379

# --- Reverse proxy (Docker full stack only) ---------------------------------
# Public port the nginx service publishes; routes /api -> backend, / -> frontend.
NGINX_PORT=80

# --- Observability (OpenTelemetry tracing; optional) ------------------------
OTEL_ENABLED=false
OTEL_SERVICE_NAME=polymart-backend
OTEL_EXPORTER_OTLP_ENDPOINT=

# --- Frontend ---------------------------------------------------------------
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
